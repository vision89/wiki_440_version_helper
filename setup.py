from setuptools import setup, find_packages

setup(
    name='wiki_440_version_helper',
    version='0.1',
    packages=find_packages(exclude=['tests*', 'venv*']),
    url = '',
    author='dlgulley411',
    author_email='',
    description='A small package for a class project'
)